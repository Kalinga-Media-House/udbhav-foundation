diff --git a/src/app/gallery/[slug]/page.tsx b/src/app/gallery/[slug]/page.tsx
index 057bb8e..ecf48bd 100644
--- a/src/app/gallery/[slug]/page.tsx
+++ b/src/app/gallery/[slug]/page.tsx
@@ -51 +51 @@ export default async function AlbumDetailPage(props: { params: Promise<{ slug: s
-    .filter((item) => item.media?.public_url)
+    .filter((item) => item.media?.cdn_url)
@@ -64,2 +64,2 @@ export default async function AlbumDetailPage(props: { params: Promise<{ slug: s
-        imageUrl: item.media!.public_url!,
-        title: item.title || album.title,
+        imageUrl: item.media!.cdn_url!,
+        title: item.caption || album.title,
@@ -67 +67 @@ export default async function AlbumDetailPage(props: { params: Promise<{ slug: s
-        altText: item.media?.alt_text || item.title || album.title,
+        altText: item.media?.alt_text || item.caption || album.title,
diff --git a/src/app/news-and-stories/[slug]/page.tsx b/src/app/news-and-stories/[slug]/page.tsx
index eaa8a28..ccd4cd4 100644
--- a/src/app/news-and-stories/[slug]/page.tsx
+++ b/src/app/news-and-stories/[slug]/page.tsx
@@ -40 +40 @@ export async function generateMetadata({ params }: PageProps): Promise<Metadata>
-  const imageUrl = article.cover_image?.public_url || '/images/default-news-cover.jpg';
+  const imageUrl = article.cover_image?.cdn_url || '/images/default-news-cover.jpg';
@@ -107 +107 @@ export default async function NewsArticlePage({ params }: PageProps) {
-    image: article.cover_image?.public_url ? [article.cover_image.public_url] : [],
+    image: article.cover_image?.cdn_url ? [article.cover_image.cdn_url] : [],
@@ -203 +203 @@ export default async function NewsArticlePage({ params }: PageProps) {
-        {article.cover_image?.public_url && (
+        {article.cover_image?.cdn_url && (
@@ -207 +207 @@ export default async function NewsArticlePage({ params }: PageProps) {
-                src={article.cover_image.public_url}
+                src={article.cover_image.cdn_url}
@@ -308 +308 @@ export default async function NewsArticlePage({ params }: PageProps) {
-                      {rel.cover_image?.public_url && (
+                      {rel.cover_image?.cdn_url && (
@@ -311 +311 @@ export default async function NewsArticlePage({ params }: PageProps) {
-                            src={rel.cover_image.public_url}
+                            src={rel.cover_image.cdn_url}
diff --git a/src/app/programmes/[slug]/page.tsx b/src/app/programmes/[slug]/page.tsx
index 67f6a4f..2e2fd6a 100644
--- a/src/app/programmes/[slug]/page.tsx
+++ b/src/app/programmes/[slug]/page.tsx
@@ -28 +28 @@ export async function generateMetadata({ params }: PageProps): Promise<Metadata>
-      title = `${programmeRow.display_order.toString().padStart(2, '0')}: ${programmeRow.title} | UDBHAV FOUNDATION`;
+      title = `${(programmeRow.display_order ?? 0).toString().padStart(2, '0')}: ${programmeRow.title} | UDBHAV FOUNDATION`;
@@ -63 +63 @@ export default async function ProgrammeDetailPage({ params }: PageProps) {
-      programmeNumber: p.display_order.toString().padStart(2, '0'),
+      programmeNumber: (p.display_order ?? 0).toString().padStart(2, '0'),
@@ -108 +108 @@ export default async function ProgrammeDetailPage({ params }: PageProps) {
-          programmeNumber: (p.display_order ?? 0).toString().padStart(2, '0'),
+          programmeNumber: ((p.display_order ?? 0) ?? 0).toString().padStart(2, '0'),
diff --git a/src/app/programmes/page.tsx b/src/app/programmes/page.tsx
index 2a7b4a0..87adda7 100644
--- a/src/app/programmes/page.tsx
+++ b/src/app/programmes/page.tsx
@@ -26 +26 @@ export default async function IndexPage() {
-    const meta = p.metadata || {};
+    const meta = (p.metadata || {}) as any;
@@ -29 +29 @@ export default async function IndexPage() {
-      programmeNumber: p.display_order.toString().padStart(2, '0'),
+      programmeNumber: (p.display_order ?? 0).toString().padStart(2, '0'),
diff --git a/src/components/admin/GalleryItemManager.tsx b/src/components/admin/GalleryItemManager.tsx
index 16d14d3..ae7ae62 100644
--- a/src/components/admin/GalleryItemManager.tsx
+++ b/src/components/admin/GalleryItemManager.tsx
@@ -179 +179 @@ export function GalleryItemManager({ albumId, albumTitle, initialItems }: Galler
-                  {item.media?.public_url ? (
+                  {item.media?.cdn_url ? (
@@ -181,2 +181,2 @@ export function GalleryItemManager({ albumId, albumTitle, initialItems }: Galler
-                      src={item.media.public_url}
-                      alt={item.media.alt_text || item.title || 'Album photo'}
+                      src={item.media.cdn_url}
+                      alt={item.media.alt_text || item.caption || 'Album photo'}
@@ -200 +200 @@ export function GalleryItemManager({ albumId, albumTitle, initialItems }: Galler
-                    {item.title && <p className="text-xs font-semibold text-gray-800 line-clamp-1">{item.title}</p>}
+                    {item.caption && <p className="text-xs font-semibold text-gray-800 line-clamp-1">{item.caption}</p>}
diff --git a/src/components/admin/ProgramForm.tsx b/src/components/admin/ProgramForm.tsx
index f014e09..8ba879d 100644
--- a/src/components/admin/ProgramForm.tsx
+++ b/src/components/admin/ProgramForm.tsx
@@ -86 +86 @@ export function ProgramForm({ initialData }: ProgramFormProps) {
-          metadata: initialData?.metadata || {},
+          metadata: (initialData?.metadata as Record<string, unknown>) || {},
diff --git a/src/components/admin/index/AdminInitiativeManagerClient.tsx b/src/components/admin/index/AdminInitiativeManagerClient.tsx
index 766b1bb..50b7c58 100644
--- a/src/components/admin/index/AdminInitiativeManagerClient.tsx
+++ b/src/components/admin/index/AdminInitiativeManagerClient.tsx
@@ -81 +81 @@ export function AdminInitiativeManagerClient({ initialInitiatives }: AdminInitia
-        setGalleryUrls(item.gallery.map(g => ({ id: g.media_id, public_url: g.public_url! })));
+        setGalleryUrls(item.gallery.map(g => ({ id: g.media_id, public_url: g.cdn_url! })));
@@ -126 +126 @@ export function AdminInitiativeManagerClient({ initialInitiatives }: AdminInitia
-      setCoverMediaUrl(res.data.public_url);
+      setCoverMediaUrl(res.data.cdn_url);
@@ -146 +146 @@ export function AdminInitiativeManagerClient({ initialInitiatives }: AdminInitia
-        setGalleryUrls(prev => [...prev, { id: res.data!.id, public_url: res.data!.public_url! }]);
+        setGalleryUrls(prev => [...prev, { id: res.data!.id, public_url: res.data!.cdn_url! }]);
diff --git a/src/components/admin/roles/RoleManager.tsx b/src/components/admin/roles/RoleManager.tsx
index 04d5255..4bbbf07 100644
--- a/src/components/admin/roles/RoleManager.tsx
+++ b/src/components/admin/roles/RoleManager.tsx
@@ -47 +47 @@ export function RoleManager() {
-        return [...prev, { role_id: roleId, permission_id: permissionId }];
+        return [...prev, { role_id: roleId, permission_id: permissionId, created_at: new Date().toISOString(), created_by: null }];
@@ -70 +70 @@ export function RoleManager() {
-      const row: any = { Role: role.name };
+      const row: any = { Role: role.display_name };
@@ -72 +72 @@ export function RoleManager() {
-        row[p.name] = rolePerms.some((rp) => rp.role_id === role.id && rp.permission_id === p.id)
+        row[p.display_name] = rolePerms.some((rp) => rp.role_id === role.id && rp.permission_id === p.id)
@@ -104 +104 @@ export function RoleManager() {
-                  title={p.description || p.name}
+                  title={p.description || p.display_name}
@@ -106 +106 @@ export function RoleManager() {
-                  {p.name}
+                  {p.display_name}
@@ -115 +115 @@ export function RoleManager() {
-                <td className="border border-gray-200 px-4 py-2 font-medium">{role.name}</td>
+                <td className="border border-gray-200 px-4 py-2 font-medium">{role.display_name}</td>
diff --git a/src/components/index/InitiativeDetailClient.tsx b/src/components/index/InitiativeDetailClient.tsx
index 381a634..1f71b07 100644
--- a/src/components/index/InitiativeDetailClient.tsx
+++ b/src/components/index/InitiativeDetailClient.tsx
@@ -189 +189 @@ export function InitiativeDetailClient({ initiative, related, adjacent }: Initia
-              const url = photo.public_url || '/hero/hero-02.png';
+              const url = photo.cdn_url || '/hero/hero-02.png';
@@ -257 +257 @@ export function InitiativeDetailClient({ initiative, related, adjacent }: Initia
-                  src={gallery[lightboxIndex]!.public_url || '/hero/hero-02.png'}
+                  src={gallery[lightboxIndex]!.cdn_url || '/hero/hero-02.png'}
diff --git a/src/components/news-and-stories/ImpactStoriesSection.tsx b/src/components/news-and-stories/ImpactStoriesSection.tsx
index 79de951..5625591 100644
--- a/src/components/news-and-stories/ImpactStoriesSection.tsx
+++ b/src/components/news-and-stories/ImpactStoriesSection.tsx
@@ -38 +38 @@ export function ImpactStoriesSection({ articles }: ImpactStoriesSectionProps = {
-          imageUrl: a.cover_image?.public_url || "/images/default-news-cover.jpg",
+          imageUrl: a.cover_image?.cdn_url || "/images/default-news-cover.jpg",
diff --git a/src/components/news-and-stories/LatestUpdatesSection.tsx b/src/components/news-and-stories/LatestUpdatesSection.tsx
index 7ff4d5d..150d25b 100644
--- a/src/components/news-and-stories/LatestUpdatesSection.tsx
+++ b/src/components/news-and-stories/LatestUpdatesSection.tsx
@@ -49 +49 @@ export function LatestUpdatesSection({ articles }: LatestUpdatesSectionProps = {
-        coverImageUrl: a.cover_image?.public_url || '/images/default-news-cover.jpg',
+        coverImageUrl: a.cover_image?.cdn_url || '/images/default-news-cover.jpg',
diff --git a/src/components/volunteers/VolunteerDashboardClient.tsx b/src/components/volunteers/VolunteerDashboardClient.tsx
index 543f82e..a8fb322 100644
--- a/src/components/volunteers/VolunteerDashboardClient.tsx
+++ b/src/components/volunteers/VolunteerDashboardClient.tsx
@@ -22 +22 @@ export function VolunteerDashboardClient({ initialData }: Props) {
-    Array.isArray(volunteer.metadata?.activity_logs) ? (volunteer.metadata.activity_logs as any) : [];
+    Array.isArray((volunteer.metadata as any)?.activity_logs) ? ((volunteer.metadata as any)?.activity_logs as any) : [];
diff --git a/src/features/analytics/repository.ts b/src/features/analytics/repository.ts
index 1e10671..4d419bc 100644
--- a/src/features/analytics/repository.ts
+++ b/src/features/analytics/repository.ts
@@ -154 +154 @@ export class AnalyticsRepository {
-      .in('status', ['Open', 'Pending', 'open', 'pending'])
+      .in('status', ['Open', 'Pending'])
diff --git a/src/features/audit_logs/repository.ts b/src/features/audit_logs/repository.ts
index d042b0a..d4d152a 100644
--- a/src/features/audit_logs/repository.ts
+++ b/src/features/audit_logs/repository.ts
@@ -5,0 +6 @@ import type { Pagination } from '@/types';
+import type { Database } from '@/types/database/database.generated';
@@ -10,17 +11 @@ export type ActivityCategory = 'Authentication' | 'Authorization' | 'Users' | 'P
-export type ActivityLogRow = {
-  id: string;
-  actor_id: string | null;
-  action: string;
-  category: ActivityCategory;
-  module: string;
-  severity: ActivitySeverity;
-  description: string | null;
-  entity_type: string | null;
-  entity_id: string | null;
-  old_values: Record<string, unknown> | null;
-  new_values: Record<string, unknown> | null;
-  metadata: Record<string, unknown>;
-  ip_address: string | null;
-  user_agent: string | null;
-  created_at: string;
-};
+export type ActivityLogRow = Database['public']['Tables']['activity_logs']['Row'];
@@ -38,2 +23,2 @@ export class AuditLogsRepository {
-    if (filters?.category) query = query.eq('category', filters.category as string);
-    if (filters?.severity) query = query.eq('severity', filters.severity as string);
+    if (filters?.category) query = query.eq('category', filters.category as any);
+    if (filters?.severity) query = query.eq('severity', filters.severity as any);
diff --git a/src/features/automation/actions.ts b/src/features/automation/actions.ts
index 8497b5c..2876e50 100644
--- a/src/features/automation/actions.ts
+++ b/src/features/automation/actions.ts
@@ -3 +3 @@
-import { requireAuth } from '@/lib/api/handler';
+import { requireAuth } from '@/contracts/actions';
@@ -14 +14 @@ export async function processQueue() {
-  await requireAuth(['admin', 'super-admin']);
+  await requireAuth();
@@ -30 +30 @@ export async function getFailedEmails() {
-  await requireAuth(['admin', 'super-admin']);
+  await requireAuth();
@@ -35,2 +35,2 @@ export async function getUnreadNotifications() {
-  const { user } = await requireAuth();
-  return NotificationRepository.getUnreadNotificationsForUser(user.id);
+  const session = await requireAuth();
+  return NotificationRepository.getUnreadNotificationsForUser(session.id);
@@ -40,2 +40,2 @@ export async function markNotificationAsRead(notificationId: string) {
-  const { user } = await requireAuth();
-  await NotificationRepository.markAsRead(notificationId, user.id);
+  const session = await requireAuth();
+  await NotificationRepository.markAsRead(notificationId, session.id);
@@ -46,2 +46,2 @@ export async function markAllNotificationsAsRead() {
-  const { user } = await requireAuth();
-  await NotificationRepository.markAllAsRead(user.id);
+  const session = await requireAuth();
+  await NotificationRepository.markAllAsRead(session.id);
diff --git a/src/features/contacts/repository.ts b/src/features/contacts/repository.ts
index 1bc38ef..461929b 100644
--- a/src/features/contacts/repository.ts
+++ b/src/features/contacts/repository.ts
@@ -5,0 +6 @@ import type { Pagination, ID } from '@/types';
+import type { Database } from '@/types/database/database.generated';
@@ -7,29 +8 @@ import type { Pagination, ID } from '@/types';
-export type EnquiryRow = {
-  id: string;
-  enquiry_number: string;
-  contact_id: string;
-  subject: string;
-  message: string;
-  department: string;
-  category: string;
-  priority: string;
-  status: string;
-  source: string;
-  channel: string | null;
-  assigned_to: string | null;
-  assigned_by: string | null;
-  assignment_time: string | null;
-  resolved_by: string | null;
-  resolved_at: string | null;
-  first_response_time: string | null;
-  expected_resolution: string | null;
-  escalation_level: number;
-  ip_address: string | null;
-  user_agent: string | null;
-  metadata: Record<string, unknown>;
-  created_by: string | null;
-  updated_by: string | null;
-  created_at: string;
-  updated_at: string;
-  is_deleted: boolean;
-};
+export type EnquiryRow = Database['public']['Tables']['enquiries']['Row'];
@@ -42,19 +15 @@ export type EnquiryCreate = Omit<
-export type OrganizationRow = {
-  id: string;
-  org_number: string;
-  parent_organization_id: string | null;
-  name: string;
-  organization_type: string;
-  website: string | null;
-  email: string | null;
-  phone: string | null;
-  address: string | null;
-  district: string | null;
-  state: string | null;
-  country: string | null;
-  logo_media_id: string | null;
-  status: string;
-  is_deleted: boolean;
-  created_at: string;
-  updated_at: string;
-};
+export type OrganizationRow = Database['public']['Tables']['organizations']['Row'];
@@ -72,28 +27 @@ export type ContactTypeRow = {
-export type ContactRow = {
-  id: string;
-  contact_number: string;
-  profile_id: string | null;
-  full_name: string;
-  organization_id: string | null;
-  designation: string | null;
-  email: string | null;
-  phone: string | null;
-  alternate_phone: string | null;
-  address: string | null;
-  city: string | null;
-  district: string | null;
-  state: string | null;
-  country: string | null;
-  website: string | null;
-  social_links: Record<string, unknown>;
-  preferred_contact_method: string | null;
-  preferred_language: string | null;
-  photo_media_id: string | null;
-  status: string;
-  is_deleted: boolean;
-  notes: string | null;
-  created_by: string | null;
-  updated_by: string | null;
-  created_at: string;
-  updated_at: string;
-};
+export type ContactRow = Database['public']['Tables']['contacts']['Row'];
@@ -181,3 +109,3 @@ export class ContactsRepository {
-    if (filters?.status) query = query.eq('status', filters.status as string);
-    if (filters?.department) query = query.eq('department', filters.department as string);
-    if (filters?.priority) query = query.eq('priority', filters.priority as string);
+    if (filters?.status) query = query.eq('status', filters.status as any);
+    if (filters?.department) query = query.eq('department', filters.department as any);
+    if (filters?.priority) query = query.eq('priority', filters.priority as any);
@@ -289 +217 @@ export class ContactsRepository {
-    if (filters?.status) query = query.eq('status', filters.status as string);
+    if (filters?.status) query = query.eq('status', filters.status as any);
@@ -339 +267 @@ export class ContactsRepository {
-    if (filters?.status) query = query.eq('status', filters.status as string);
+    if (filters?.status) query = query.eq('status', filters.status as any);
@@ -389 +317 @@ export class ContactsRepository {
-        await (supabase.from(table) as any).update({ contact_id: survivingId }).eq('contact_id', deletedId);
+        await (supabase.from(table as any) as any).update({ contact_id: survivingId }).eq('contact_id', deletedId);
diff --git a/src/features/contacts/service.ts b/src/features/contacts/service.ts
index 26cec7c..bb8ac77 100644
--- a/src/features/contacts/service.ts
+++ b/src/features/contacts/service.ts
@@ -62 +62 @@ export class ContactsService {
-    const contactData: ContactCreate = {
+    const contactData = {
@@ -83 +83 @@ export class ContactsService {
-    return fromRepo(await contactsRepository.createContact(contactData));
+    return fromRepo(await contactsRepository.createContact(contactData as any));
@@ -103 +103 @@ export class ContactsService {
-    const orgData: OrganizationCreate = {
+    const orgData = {
@@ -117 +117 @@ export class ContactsService {
-    return fromRepo(await contactsRepository.createOrganization(orgData));
+    return fromRepo(await contactsRepository.createOrganization(orgData as any));
diff --git a/src/features/donations/repository.ts b/src/features/donations/repository.ts
index 11885e8..59885a1 100644
--- a/src/features/donations/repository.ts
+++ b/src/features/donations/repository.ts
@@ -5,0 +6 @@ import type { Pagination, ID } from '@/types';
+import type { Database } from '@/types/database/database.generated';
@@ -30 +31 @@ export type DonationRow = {
-  metadata: Record<string, unknown>;
+  metadata: Database['public']['Tables']['donation_campaigns']['Row']['metadata'];
@@ -65 +66 @@ export type CampaignRow = {
-  metadata: Record<string, unknown>;
+  metadata: Database['public']['Tables']['donation_campaigns']['Row']['metadata'];
@@ -109 +110 @@ export class DonationsRepository {
-    if (filters?.status) query = query.eq('status', filters.status as string);
+    if (filters?.status) query = query.eq('status', filters.status as any);
diff --git a/src/features/gallery/repository.ts b/src/features/gallery/repository.ts
index c89a4b5..b905bcf 100644
--- a/src/features/gallery/repository.ts
+++ b/src/features/gallery/repository.ts
@@ -5,0 +6 @@ import type { Pagination, ID } from '@/types';
+import type { Database } from '@/types/database/database.generated';
@@ -10,18 +11,2 @@ import type { Pagination, ID } from '@/types';
-export type AlbumRow = {
-  id: string;
-  album_code: string;
-  slug: string;
-  title: string;
-  description: string | null;
-  cover_image_id: string | null;
-  program_id: string | null;
-  event_id: string | null;
-  visibility: string;
-  display_order: number;
-  is_featured: boolean;
-  item_count: number;
-  created_by: string | null;
-  updated_by: string | null;
-  created_at: string;
-  updated_at: string;
-  is_deleted: boolean;
+export type AlbumRow = Database['public']['Tables']['gallery_albums']['Row'] & {
+  item_count?: number;
@@ -41,14 +26 @@ export type AlbumUpdate = Partial<Omit<AlbumCreate, 'album_code'>>;
-export type GalleryItemRow = {
-  id: string;
-  album_id: string;
-  media_id: string;
-  title: string | null;
-  caption: string | null;
-  display_order: number;
-  is_featured: boolean;
-  created_by: string | null;
-  updated_by: string | null;
-  created_at: string;
-  updated_at: string;
-  is_deleted: boolean;
-};
+export type GalleryItemRow = Database['public']['Tables']['gallery_items']['Row'];
@@ -59 +31 @@ export type GalleryItemWithMedia = GalleryItemRow & {
-    public_url: string | null;
+    cdn_url: string | null;
@@ -62 +34 @@ export type GalleryItemWithMedia = GalleryItemRow & {
-    title: string | null;
+    
@@ -118,3 +90,3 @@ export class GalleryRepository implements IWriteRepository<AlbumRow, AlbumCreate
-    if (filters?.status) query = query.eq('status', filters.status as string);
-    if (filters?.album_type) query = query.eq('album_type', filters.album_type as string);
-    if (filters?.visibility) query = query.eq('visibility', filters.visibility as string);
+    if (filters?.status) query = query.eq('status', filters.status as any);
+    if (filters?.album_type) query = query.eq('album_type', filters.album_type as any);
+    if (filters?.visibility) query = query.eq('visibility', filters.visibility as any);
@@ -156 +128 @@ export class GalleryRepository implements IWriteRepository<AlbumRow, AlbumCreate
-      const { data: row, error } = await (supabase.from('gallery_albums') as any).update(data as any).eq('id', id).eq('is_deleted', false).select().single();
+      const { data: row, error } = await (supabase.from('gallery_albums') as any).update(data as any).eq('id', id).select().single();
@@ -257 +229 @@ export class GalleryRepository implements IWriteRepository<AlbumRow, AlbumCreate
-    const { data, count, error } = await supabase.from('gallery_items').select('*', { count: 'exact' }).eq('album_id', albumId).eq('is_deleted', false).order('display_order', { ascending: true }).range(from, from + pagination.limit - 1);
+    const { data, count, error } = await supabase.from('gallery_items').select('*', { count: 'exact' }).eq('album_id', albumId).order('display_order', { ascending: true }).range(from, from + pagination.limit - 1);
@@ -263 +235 @@ export class GalleryRepository implements IWriteRepository<AlbumRow, AlbumCreate
-   * Lists items for a gallery album enriched with media_objects data.
+   * Lists items for a gallery album enriched with media_files data.
@@ -275 +246,0 @@ export class GalleryRepository implements IWriteRepository<AlbumRow, AlbumCreate
-      .eq('is_deleted', false)
@@ -290 +261 @@ export class GalleryRepository implements IWriteRepository<AlbumRow, AlbumCreate
-    const mediaIds = items.map((item) => item.media_id);
+    const mediaIds = items.map((item) => item.media_file_id);
@@ -292,2 +263,2 @@ export class GalleryRepository implements IWriteRepository<AlbumRow, AlbumCreate
-      .from('media_objects')
-      .select('id, public_url, alt_text, caption, title, width, height')
+      .from('media_files')
+      .select('id, cdn_url, alt_text, caption, width, height')
@@ -296 +267 @@ export class GalleryRepository implements IWriteRepository<AlbumRow, AlbumCreate
-    const mediaList = (rawMediaList as { id: string; public_url: string | null; alt_text: string | null; caption: string | null; title: string | null; width: number | null; height: number | null }[]) ?? [];
+    const mediaList = (rawMediaList as { id: string; cdn_url: string | null; alt_text: string | null; caption: string | null;  width: number | null; height: number | null }[]) ?? [];
@@ -301 +272 @@ export class GalleryRepository implements IWriteRepository<AlbumRow, AlbumCreate
-      media: mediaMap.get(item.media_id) || null,
+      media: mediaMap.get(item.media_file_id) || null,
diff --git a/src/features/gallery/service.ts b/src/features/gallery/service.ts
index 047c8f4..57c3f9e 100644
--- a/src/features/gallery/service.ts
+++ b/src/features/gallery/service.ts
@@ -52 +52 @@ export class GalleryService {
-    return fromRepo(await galleryRepository.create({ ...parsed.data, created_by: userId, updated_by: userId } as AlbumCreate));
+    return fromRepo(await galleryRepository.create({ ...parsed.data as any, created_by: userId, updated_by: userId } as AlbumCreate));
@@ -65 +65 @@ export class GalleryService {
-    return fromRepo(await galleryRepository.update(id, { ...parsed.data, updated_by: userId }));
+    return fromRepo(await galleryRepository.update(id, { ...parsed.data as any, updated_by: userId }));
@@ -97 +97 @@ export class GalleryService {
-    return fromRepo(await galleryRepository.addItem({ ...parsed.data, created_by: userId, updated_by: userId } as GalleryItemCreate));
+    return fromRepo(await galleryRepository.addItem({ ...parsed.data as any, created_by: userId, updated_by: userId } as any));
diff --git a/src/features/index/repository.ts b/src/features/index/repository.ts
index 140afc4..370f47d 100644
--- a/src/features/index/repository.ts
+++ b/src/features/index/repository.ts
@@ -54 +54 @@ export type IndexInitiativeWithMedia = IndexInitiativeRow & {
-    public_url: string | null;
+    cdn_url: string | null;
@@ -281 +281 @@ export class IndexInitiativesRepository
-      const mediaMap = new Map<string, { public_url: string | null; alt_text: string | null }>();
+      const mediaMap = new Map<string, { cdn_url: string | null; alt_text: string | null }>();
@@ -286 +286 @@ export class IndexInitiativesRepository
-          .select('id, public_url, alt_text')
+          .select('id, cdn_url, alt_text')
@@ -289,2 +289,2 @@ export class IndexInitiativesRepository
-          coverMedia.forEach((m: { id: string; public_url: string | null; alt_text: string | null }) => {
-            mediaMap.set(m.id, { public_url: m.public_url, alt_text: m.alt_text });
+          coverMedia.forEach((m: { id: string; cdn_url: string | null; alt_text: string | null }) => {
+            mediaMap.set(m.id, { cdn_url: m.cdn_url, alt_text: m.alt_text });
@@ -305 +305 @@ export class IndexInitiativesRepository
-          .select('id, public_url, alt_text')
+          .select('id, cdn_url, alt_text')
@@ -308,2 +308,2 @@ export class IndexInitiativesRepository
-          galMedia.forEach((m: { id: string; public_url: string | null; alt_text: string | null }) => {
-            mediaMap.set(m.id, { public_url: m.public_url, alt_text: m.alt_text });
+          galMedia.forEach((m: { id: string; cdn_url: string | null; alt_text: string | null }) => {
+            mediaMap.set(m.id, { cdn_url: m.cdn_url, alt_text: m.alt_text });
@@ -314 +314 @@ export class IndexInitiativesRepository
-      const galleryMap = new Map<string, Array<{ id: string; media_id: string; caption: string | null; display_order: number; public_url: string | null; alt_text: string | null }>>();
+      const galleryMap = new Map<string, Array<{ id: string; media_id: string; caption: string | null; display_order: number; cdn_url: string | null; alt_text: string | null }>>();
@@ -322 +322 @@ export class IndexInitiativesRepository
-          public_url: media?.public_url || null,
+          cdn_url: media?.cdn_url || null,
@@ -334 +334 @@ export class IndexInitiativesRepository
-          cover_image_url: coverObj?.public_url || null,
+          cover_image_url: coverObj?.cdn_url || null,
diff --git a/src/features/lookups/repository.ts b/src/features/lookups/repository.ts
index 7e35f97..e869eda 100644
--- a/src/features/lookups/repository.ts
+++ b/src/features/lookups/repository.ts
@@ -7,14 +7 @@ import type { ID } from '@/types';
-export type TaxonomyRow = {
-  id: string;
-  slug: string;
-  display_name: string;
-  description: string | null;
-  type: 'flat' | 'hierarchical';
-  status: 'active' | 'inactive' | 'deprecated';
-  visibility: 'public' | 'internal' | 'system';
-  icon: string | null;
-  color: string | null;
-  sort_order: number;
-  metadata: Record<string, unknown>;
-  created_at: string;
-};
+import type { Database } from '@/types/database/database.generated';
@@ -22,14 +9,2 @@ export type TaxonomyRow = {
-export type TaxonomyTermRow = {
-  id: string;
-  taxonomy_id: string;
-  parent_id: string | null;
-  slug: string;
-  display_name: string;
-  description: string | null;
-  code: string | null;
-  icon: string | null;
-  color: string | null;
-  sort_order: number;
-  metadata: Record<string, unknown>;
-  created_at: string;
-};
+export type TaxonomyRow = Database['public']['Tables']['taxonomies']['Row'];
+export type TaxonomyTermRow = Database['public']['Tables']['taxonomy_terms']['Row'];
diff --git a/src/features/media/repository.ts b/src/features/media/repository.ts
index 9580e1b..d6351eb 100644
--- a/src/features/media/repository.ts
+++ b/src/features/media/repository.ts
@@ -7,21 +7,3 @@ import type { Pagination, ID } from '@/types';
-export type MediaFileRow = {
-  id: string;
-  file_name: string;
-  original_name: string;
-  file_path: string;
-  bucket_name: string;
-  public_url: string | null;
-  file_size: number;
-  mime_type: string;
-  file_type: string;
-  entity_type: string | null;
-  entity_id: string | null;
-  alt_text: string | null;
-  title: string | null;
-  description: string | null;
-  metadata: Record<string, unknown>;
-  created_by: string | null;
-  created_at: string;
-  updated_at: string;
-  is_deleted: boolean;
-};
+import type { Database } from '@/types/database/database.generated';
+
+export type MediaFileRow = Database['public']['Tables']['media_files']['Row'];
@@ -79,2 +61,2 @@ export class MediaRepository {
-      .eq('entity_type', entityType)
-      .eq('entity_id', entityId)
+      .eq('entity_type' as any, entityType)
+      .eq('entity_id' as any, entityId)
diff --git a/src/features/media/service.ts b/src/features/media/service.ts
index 8b39234..e5e063a 100644
--- a/src/features/media/service.ts
+++ b/src/features/media/service.ts
@@ -54,3 +54,2 @@ export class MediaService {
-      file_name: storageObj.key.split('/').pop() ?? originalFilename,
-      original_name: originalFilename,
-      file_path: storageObj.key,
+      original_filename: originalFilename,
+      r2_object_key: storageObj.key,
@@ -58 +57 @@ export class MediaService {
-      public_url: storageObj.url,
+      cdn_url: storageObj.url,
@@ -61,11 +60 @@ export class MediaService {
-      file_type: contentType.startsWith('image')
-        ? 'Image'
-        : contentType.startsWith('video')
-        ? 'Video'
-        : 'Document',
-      entity_type: entityType,
-      entity_id: entityId,
-      alt_text: null,
-      title: null,
-      description: null,
-      metadata: {},
+      status: 'Ready',
@@ -73 +62 @@ export class MediaService {
-    });
+    } as any);
diff --git a/src/features/news/repository.ts b/src/features/news/repository.ts
index f3c17b4..075bb28 100644
--- a/src/features/news/repository.ts
+++ b/src/features/news/repository.ts
@@ -33 +33 @@ export type ArticleWithMedia = ArticleRow & {
-    public_url: string | null;
+    cdn_url: string | null;
@@ -89,2 +89,2 @@ export class NewsRepository implements IWriteRepository<ArticleRow, ArticleCreat
-          .from('media_objects')
-          .select('id, public_url, alt_text, caption, width, height')
+          .from('media_files')
+          .select('id, cdn_url, alt_text, caption, width, height')
@@ -93 +93 @@ export class NewsRepository implements IWriteRepository<ArticleRow, ArticleCreat
-        const media = rawMedia as { id: string; public_url: string | null; alt_text: string | null; caption: string | null; width: number | null; height: number | null } | null;
+        const media = rawMedia as { id: string; cdn_url: string | null; alt_text: string | null; caption: string | null; width: number | null; height: number | null } | null;
@@ -123,2 +123,2 @@ export class NewsRepository implements IWriteRepository<ArticleRow, ArticleCreat
-          .from('media_objects')
-          .select('id, public_url, alt_text, caption, width, height')
+          .from('media_files')
+          .select('id, cdn_url, alt_text, caption, width, height')
@@ -127 +127 @@ export class NewsRepository implements IWriteRepository<ArticleRow, ArticleCreat
-        const media = rawMedia as { id: string; public_url: string | null; alt_text: string | null; caption: string | null; width: number | null; height: number | null } | null;
+        const media = rawMedia as { id: string; cdn_url: string | null; alt_text: string | null; caption: string | null; width: number | null; height: number | null } | null;
@@ -162 +162 @@ export class NewsRepository implements IWriteRepository<ArticleRow, ArticleCreat
-      query = query.eq('status', filters.status as string);
+      query = query.eq('status', filters.status as any);
@@ -168 +168 @@ export class NewsRepository implements IWriteRepository<ArticleRow, ArticleCreat
-      query = query.eq('visibility', filters.visibility as string);
+      query = query.eq('visibility', filters.visibility as any);
@@ -199 +199 @@ export class NewsRepository implements IWriteRepository<ArticleRow, ArticleCreat
-    const mediaMap = new Map<string, { id: string; public_url: string | null; alt_text: string | null; caption: string | null; width: number | null; height: number | null }>();
+    const mediaMap = new Map<string, { id: string; cdn_url: string | null; alt_text: string | null; caption: string | null; width: number | null; height: number | null }>();
@@ -203,2 +203,2 @@ export class NewsRepository implements IWriteRepository<ArticleRow, ArticleCreat
-        .from('media_objects')
-        .select('id, public_url, alt_text, caption, width, height')
+        .from('media_files')
+        .select('id, cdn_url, alt_text, caption, width, height')
@@ -207 +207 @@ export class NewsRepository implements IWriteRepository<ArticleRow, ArticleCreat
-      const mediaRows = (rawMediaRows as { id: string; public_url: string | null; alt_text: string | null; caption: string | null; width: number | null; height: number | null }[]) ?? [];
+      const mediaRows = (rawMediaRows as { id: string; cdn_url: string | null; alt_text: string | null; caption: string | null; width: number | null; height: number | null }[]) ?? [];
@@ -359 +359 @@ export class NewsRepository implements IWriteRepository<ArticleRow, ArticleCreat
-    const mediaMap = new Map<string, { id: string; public_url: string | null; alt_text: string | null; caption: string | null; width: number | null; height: number | null }>();
+    const mediaMap = new Map<string, { id: string; cdn_url: string | null; alt_text: string | null; caption: string | null; width: number | null; height: number | null }>();
@@ -363,2 +363,2 @@ export class NewsRepository implements IWriteRepository<ArticleRow, ArticleCreat
-        .from('media_objects')
-        .select('id, public_url, alt_text, caption, width, height')
+        .from('media_files')
+        .select('id, cdn_url, alt_text, caption, width, height')
@@ -367 +367 @@ export class NewsRepository implements IWriteRepository<ArticleRow, ArticleCreat
-      const mediaRows = (rawMediaRows as { id: string; public_url: string | null; alt_text: string | null; caption: string | null; width: number | null; height: number | null }[]) ?? [];
+      const mediaRows = (rawMediaRows as { id: string; cdn_url: string | null; alt_text: string | null; caption: string | null; width: number | null; height: number | null }[]) ?? [];
diff --git a/src/features/profiles/service.ts b/src/features/profiles/service.ts
index 296d92a..6a73534 100644
--- a/src/features/profiles/service.ts
+++ b/src/features/profiles/service.ts
@@ -7,13 +7,8 @@ import type { ID } from '@/types';
-export type ProfileRow = {
-  id: string;
-  email: string;
-  role: string;
-  first_name: string | null;
-  last_name: string | null;
-  phone: string | null;
-  avatar_url: string | null;
-  bio: string | null;
-  is_active: boolean;
-  metadata: Record<string, unknown>;
-  created_at: string;
-  updated_at: string;
+import type { Database } from '@/types/database/database.generated';
+
+export type ProfileRow = Database['public']['Tables']['profiles']['Row'] & {
+  email?: string;
+  role?: string;
+  avatar_url?: string | null;
+  bio?: string | null;
+  is_active?: boolean;
diff --git a/src/features/programs/repository.ts b/src/features/programs/repository.ts
index 7b05bb3..0fde8d2 100644
--- a/src/features/programs/repository.ts
+++ b/src/features/programs/repository.ts
@@ -7,20 +7,6 @@ import type { Pagination, ID } from '@/types';
-export type ProgramRow = {
-  id: string;
-  program_code: string;
-  slug: string;
-  title: string;
-  subtitle: string | null;
-  description: string | null;
-  status: string;
-  visibility: string;
-  cover_image_id: string | null;
-  start_date: string | null;
-  end_date: string | null;
-  is_featured: boolean;
-  display_order: number;
-  metadata: Record<string, unknown>;
-  created_by: string | null;
-  updated_by: string | null;
-  created_at: string;
-  updated_at: string;
-  is_deleted: boolean;
+import type { Database } from '@/types/database/database.generated';
+
+export type ProgramRow = Database['public']['Tables']['programs']['Row'] & {
+  subtitle?: string | null;
+  description?: string | null;
+  display_order?: number;
@@ -75,2 +61,2 @@ export class ProgramsRepository implements IWriteRepository<ProgramRow, ProgramC
-    if (filters?.status) query = query.eq('status', filters.status as string);
-    if (filters?.visibility) query = query.eq('visibility', filters.visibility as string);
+    if (filters?.status) query = query.eq('status', filters.status as Database['public']['Enums']['program_status']);
+    if (filters?.visibility) query = query.eq('visibility', filters.visibility as Database['public']['Enums']['program_visibility']);
diff --git a/src/features/programs/service.ts b/src/features/programs/service.ts
index 44a9503..f94ead0 100644
--- a/src/features/programs/service.ts
+++ b/src/features/programs/service.ts
@@ -10,0 +11 @@ import type { CreateProgramDTO, UpdateProgramDTO } from './validators';
+import type { Json } from '@/types/database/database.generated';
@@ -48 +49 @@ export class ProgramsService {
-      metadata: parsed.data.metadata,
+      metadata: parsed.data.metadata as any,
@@ -51 +52 @@ export class ProgramsService {
-    };
+    } as any;
@@ -61 +62,5 @@ export class ProgramsService {
-    return fromRepo(await programsRepository.update(id, { ...parsed.data, updated_by: userId }));
+    const updateData = { ...parsed.data, updated_by: userId };
+    if (updateData.metadata) {
+      updateData.metadata = updateData.metadata as any;
+    }
+    return fromRepo(await programsRepository.update(id, updateData as any));
diff --git a/src/features/reports/repository.ts b/src/features/reports/repository.ts
index 4a7b4ea..3d75618 100644
--- a/src/features/reports/repository.ts
+++ b/src/features/reports/repository.ts
@@ -26 +26 @@ export async function fetchUsers() {
-  const { data, error } = await supabase.from('users').select('*').order('created_at', { ascending: false });
+  const { data, error } = await supabase.from('profiles').select('*').order('created_at', { ascending: false });
diff --git a/src/features/roles/types.ts b/src/features/roles/types.ts
index e5b6882..57a7396 100644
--- a/src/features/roles/types.ts
+++ b/src/features/roles/types.ts
@@ -1,7 +1 @@
-∩╗┐export interface Role {
-  id: string;
-  name: string;
-  description: string | null;
-  created_at?: string;
-  updated_at?: string;
-}
+import type { Database } from '@/types/database/database.generated';
@@ -9,12 +3,3 @@
-export interface Permission {
-  id: string;
-  name: string;
-  description: string | null;
-  created_at?: string;
-  updated_at?: string;
-}
-
-export interface RolePermission {
-  role_id: string;
-  permission_id: string;
-}
+export type Role = Database['public']['Tables']['roles']['Row'];
+export type Permission = Database['public']['Tables']['permissions']['Row'];
+export type RolePermission = Database['public']['Tables']['role_permissions']['Row'];
diff --git a/src/features/search/repository.ts b/src/features/search/repository.ts
index 347923a..b56398c 100644
--- a/src/features/search/repository.ts
+++ b/src/features/search/repository.ts
@@ -29,2 +29,2 @@ export class SearchRepository {
-        supabase.from('campaigns').select('id, title').ilike('title', `%${query}%`).limit(5),
-        supabase.from('media').select('id, file_name, title').ilike('title', `%${query}%`).limit(5),
+        supabase.from('donation_campaigns').select('id, title').ilike('title', `%${query}%`).limit(5),
+        supabase.from('media_files').select('id, original_filename').ilike('original_filename', `%${query}%`).limit(5),
diff --git a/src/features/volunteers/repository.ts b/src/features/volunteers/repository.ts
index ae1982d..60c07b9 100644
--- a/src/features/volunteers/repository.ts
+++ b/src/features/volunteers/repository.ts
@@ -8,16 +8,8 @@ import type { Pagination, ID } from '@/types';
-export type VolunteerRow = {
-  id: string;
-  profile_id: string;
-  volunteer_code: string;
-  status: string;
-  total_hours: number;
-  event_count: number;
-  bio: string | null;
-  motivation: string | null;
-  availability: string | null;
-  metadata: Record<string, unknown>;
-  created_by: string | null;
-  updated_by: string | null;
-  created_at: string;
-  updated_at: string;
-  is_deleted: boolean;
+import type { Database } from '@/types/database/database.generated';
+
+export type VolunteerRow = Database['public']['Tables']['volunteers']['Row'] & {
+  total_hours?: number;
+  event_count?: number;
+  bio?: string | null;
+  motivation?: string | null;
+  availability?: string | null;
@@ -26,2 +18,2 @@ export type VolunteerRow = {
-export type VolunteerCreate = Omit<VolunteerRow, 'id' | 'created_at' | 'updated_at' | 'is_deleted' | 'total_hours' | 'event_count'>;
-export type VolunteerUpdate = Partial<Omit<VolunteerCreate, 'volunteer_code' | 'profile_id'>>;
+export type VolunteerCreate = Database['public']['Tables']['volunteers']['Insert'];
+export type VolunteerUpdate = Database['public']['Tables']['volunteers']['Update'];
@@ -134 +126 @@ export class VolunteersRepository implements IWriteRepository<VolunteerRow, Volu
-    if (filters?.status) query = query.eq('status', filters.status as string);
+    if (filters?.status) query = query.eq('status', filters.status as any);
@@ -310 +302 @@ export class VolunteersRepository implements IWriteRepository<VolunteerRow, Volu
-      .in('status', ['Active', 'Verified', 'Approved']);
+      .in('status', ['Active', 'Verified']);
diff --git a/src/features/volunteers/service.ts b/src/features/volunteers/service.ts
index df32d1a..27bba2e 100644
--- a/src/features/volunteers/service.ts
+++ b/src/features/volunteers/service.ts
@@ -68 +68,5 @@ export class VolunteersService {
-    return fromRepo(await volunteersRepository.create({ ...parsed.data, created_by: userId, updated_by: userId } as VolunteerCreate));
+    const createData = { ...parsed.data, created_by: userId, updated_by: userId };
+    if (createData.metadata) {
+      createData.metadata = createData.metadata as any;
+    }
+    return fromRepo(await volunteersRepository.create(createData as VolunteerCreate));
@@ -91 +95,5 @@ export class VolunteersService {
-    return fromRepo(await volunteersRepository.update(id, { ...parsed.data, updated_by: userId }));
+    const updateData = { ...parsed.data, updated_by: userId };
+    if (updateData.metadata) {
+      updateData.metadata = updateData.metadata as any;
+    }
+    return fromRepo(await volunteersRepository.update(id, updateData as any));
@@ -180 +188 @@ export class VolunteersService {
-          const volCreate: VolunteerCreate = {
+          const volCreate = {
@@ -184,2 +192 @@ export class VolunteersService {
-            bio: application.motivation,
-            motivation: application.motivation,
+            biography: application.motivation,
@@ -194 +201 @@ export class VolunteersService {
-            },
+            } as any,
@@ -198 +205 @@ export class VolunteersService {
-          await volunteersRepository.create(volCreate);
+          await volunteersRepository.create(volCreate as any);
diff --git a/src/types/database/index.ts b/src/types/database/index.ts
index 1acfd72..1b9aaa3 100644
--- a/src/types/database/index.ts
+++ b/src/types/database/index.ts
@@ -20 +19,0 @@ export type {
-  SuccessStoryRow,
@@ -26 +24,0 @@ export type {
-  DonorRow,
@@ -29,2 +26,0 @@ export type {
-  AuditLogRow,
-  LookupTaxonomyRow,
diff --git a/src/types/database/tables.ts b/src/types/database/tables.ts
index 4ec80e3..1ae2cd0 100644
--- a/src/types/database/tables.ts
+++ b/src/types/database/tables.ts
@@ -24 +24 @@ export type EventRow = Tables<'events'>;
- * Row type alias for the `volunteer_profiles` table.
+ * Row type alias for the `volunteers` table.
@@ -26 +26 @@ export type EventRow = Tables<'events'>;
-export type VolunteerProfileRow = Tables<'volunteer_profiles'>;
+export type VolunteerProfileRow = Tables<'volunteers'>;
@@ -39 +39 @@ export type NewsArticleRow = Tables<'news_articles'>;
- * Row type alias for the `success_stories` table.
+ * Row type alias for the `success_stories` table (legacy).
@@ -41 +41 @@ export type NewsArticleRow = Tables<'news_articles'>;
-export type SuccessStoryRow = Tables<'success_stories'>;
+// export type SuccessStoryRow = Tables<'success_stories'>;
@@ -54 +54 @@ export type GalleryItemRow = Tables<'gallery_items'>;
- * Row type alias for the `media_objects` table.
+ * Row type alias for the `media_files` table.
@@ -56 +56 @@ export type GalleryItemRow = Tables<'gallery_items'>;
-export type MediaObjectRow = Tables<'media_objects'>;
+export type MediaObjectRow = Tables<'media_files'>;
@@ -69 +69 @@ export type DonationCampaignRow = Tables<'donation_campaigns'>;
- * Row type alias for the `donors` table.
+ * Row type alias for the `donors` table (legacy).
@@ -71 +71 @@ export type DonationCampaignRow = Tables<'donation_campaigns'>;
-export type DonorRow = Tables<'donors'>;
+// export type DonorRow = Tables<'donors'>;
@@ -77,0 +78,5 @@ export type ContactRow = Tables<'contacts'>;
+/**
+ * Row type alias for the `organizations` table.
+ */
+export type OrganizationRow = Tables<'organizations'>;
+
@@ -84 +89 @@ export type EnquiryRow = Tables<'enquiries'>;
- * Row type alias for the `audit_logs` table.
+ * Row type alias for the `audit_logs` table (legacy).
@@ -86 +91 @@ export type EnquiryRow = Tables<'enquiries'>;
-export type AuditLogRow = Tables<'audit_logs'>;
+// export type AuditLogRow = Tables<'audit_logs'>;
@@ -89 +94 @@ export type AuditLogRow = Tables<'audit_logs'>;
- * Row type alias for the `lookup_taxonomy` table.
+ * Row type alias for the `lookup_taxonomy` table (legacy).
@@ -91 +96 @@ export type AuditLogRow = Tables<'audit_logs'>;
-export type LookupTaxonomyRow = Tables<'lookup_taxonomy'>;
+// export type LookupTaxonomyRow = Tables<'lookup_taxonomy'>;
