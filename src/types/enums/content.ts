/**
 * Visibility enumeration for general content.
 */
export enum VisibilityEnum {
  PUBLIC = 'public',
  PRIVATE = 'private',
  INTERNAL = 'internal',
}

/**
 * Media type enumeration for files and assets.
 */
export enum MediaTypeEnum {
  IMAGE = 'image',
  VIDEO = 'video',
  DOCUMENT = 'document',
  AUDIO = 'audio',
}

/**
 * Visibility enumeration specifically for galleries.
 */
export enum GalleryVisibilityEnum {
  PUBLIC = 'public',
  PRIVATE = 'private',
  INTERNAL = 'internal',
}

/**
 * Status enumeration for articles and news items.
 */
export enum ArticleStatusEnum {
  DRAFT = 'Draft',
  PUBLISHED = 'Published',
  ARCHIVED = 'Archived',
  SCHEDULED = 'Scheduled',
}

/**
 * Enumeration of UDBHAV Foundation program types.
 */
export enum ProgramTypeEnum {
  EDUCATION = 'Education',
  HEALTHCARE = 'Healthcare',
  ENVIRONMENT = 'Environment',
  LIVELIHOOD = 'Livelihood',
  DISASTER_RELIEF = 'Disaster Relief',
  OTHER = 'Other',
}

/**
 * Enumeration of event types.
 */
export enum EventTypeEnum {
  WORKSHOP = 'Workshop',
  SEMINAR = 'Seminar',
  DRIVE = 'Drive',
  CAMP = 'Camp',
  FUNDRAISER = 'Fundraiser',
  MEETUP = 'Meetup',
  WEBINAR = 'Webinar',
}
